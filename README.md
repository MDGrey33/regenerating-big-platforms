# Regenerating Big Platforms — a visual guide

How a heavy, long-lived brownfield platform can capture the AI-regeneration "50×":
build a foundry that converts the platform into greenfield-shaped slices one at a time,
behind manufactured spec / contracts / tests / schema, and only where the economics clear.

**Read it:** open `index.html` (also published via GitHub Pages).

15 diagrams, one concept per panel, each with an in-depth explanation. Self-contained, no JavaScript, no external dependencies.
